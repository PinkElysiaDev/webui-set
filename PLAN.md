# 风吹花海登录页 · 展示 Demo 设计文档

> 独立代码仓的完整设计与实施计划。按本文档实施；实施前请先通读
> 「素材与合规」一节。

## 1. 定位与范围

- **目标**：一个专用于展示登录效果的场景 demo——风吹动的花海与爱莉希雅的
  长发构成右侧主视觉，左侧为登录表单；昼夜切换直接改变天空色彩与光影。
- **独立仓**：位于 `D:\DevProjects\Elysia-API\login-demo\`，与 Elysia-API
  主项目互不影响（不共享构建、不共享依赖）。
- **登录规则**：密钥固定 `123`（本地校验，无后端）；成功后经转场进入
  固定首页（静态展示占位）。
- **不做**：真实鉴权、多页面、移动端深度优化（仅降级保底）。

## 2. 素材与合规

### 2.1 素材清单

| 素材 | 来源 | 用途 |
|---|---|---|
| 爱莉希雅 3.0.pmx | 解压目录（神帝宇 制作的 MMD 模型） | 主角色（默认） |
| 爱莉希雅长裙 3.0.pmx | 同上 | 变体（可选加载） |
| 法杖.pmx | 同上 | 手持饰件（可选，默认不挂） |
| 头发/头发1/头发2/脸/眼睛/表情/衣服/裙摆/裙饰/背饰/宝石/武器.png + toon/mc 贴图 | 同目录 | pmx 引用的贴图，整目录迁移 |
| task-mtzyvfxf16eyz.png | `C:\Users\xiaolan\Downloads\` | 花海背景板 |
| 花瓣 | 程序化（shader 绘制的椭圆花瓣形） | 前景粒子 |

### 2.2 合规约束（来自模型附带 使用规则.txt）

- 允许改造：优化骨骼与刚体、重制 UV ✅（本 demo 需要为发丝刚体注入风力）
- **禁止二次配布** → 资产目录 `public/assets/model/` 加入 `.gitignore`，
  仓库只放获取/放置资产的说明脚本，不提交模型与贴图本体
- **禁止商用、禁止 18 禁/极端内容** → demo 仅内部展示
- 模型版权所属 miHoYo，模型编辑：神帝宇 —— 关于页脚注明出处

## 3. 技术栈

| 层 | 选型 | 说明 |
|---|---|---|
| 构建 | Vite 5 + React 18 + TypeScript | 标准脚手架 |
| 渲染 | three.js（MMDLoader + MMDToonShader） | MMD 自带 toon 着色，即"全 shader"渲染 |
| 物理 | ammo.js（three MMDPhysics） | 长发刚体/关节模拟 |
| 后处理 | EffectComposer：Bloom + 自定义调色 pass | 昼夜光影核心 |
| UI | 手写 CSS（玻璃态）+ rAF 动效 | 不引 UI 库，保持轻量 |

依赖仅 `three`、`@types/three`、`react`、`react-dom`、`vite` 系。

## 4. 场景构图

```
┌──────────────────────────────────────────────┐
│  花海背景板（全屏，轻视差） + 天空渐变/星野        │
│                                              │
│  ┌──────────┐          ～～～～～～           │
│  │ 标题+表单  │        花瓣粒子层(前景)          │
│  │ (DOM 居左) │      ╭──────────────╮         │
│  │          │      │  爱莉希雅(MMD)  │         │
│  └──────────┘      │  长发·物理风动   │         │
│   刻印切换🌗        ╰──────────────╯         │
│                                  花丛前景带     │
└──────────────────────────────────────────────┘
```

| 参数 | 值 | 备注 |
|---|---|---|
| 画布 | 全屏 WebGL，DPR ≤ 2 | UI 为 DOM 覆盖层 |
| 相机 | 透视 35°，目标点角色胸口 | 角色占屏右 55-60% |
| 角色摆位 | x ≈ +0.35 屏宽，面向左前 15° | 与表单形成对视感 |
| 光源 | 主光（日=暖白/夜=月光蓝）+ 补光（粉）+ rim（轮廓） | 昼夜各自配色 |

## 5. 角色系统

1. **加载管线**：MMDLoader 读 pmx → MMDToonShader 材质 → 贴图透明度
   修正（MMD 乘法贴图兼容）。
2. **长发物理**：MMDPhysics（ammo.js）启用发丝刚体链；substep 默认 3，
   预算超限时降 1。
3. **风注入**：每物理步向发丝刚体施加外力 `F = wind(t, pos) × 刚体风阻
   系数`（按刚体质量归一）；风力不作用于躯干主骨骼。
4. **程序化 idle**：无外部 VMD，骨骼级程序动画——呼吸（胸部缩放 ±1%）、
   重心微摆（sin 0.1Hz ±0.5°）、头部视线跟随指针（±8° 限幅，缓动）。
5. **表情**：默认微笑 morph + 眨眼定时器（随机 3-6s）；登录成功时切换
   「开心」morph。

## 6. 风场系统（全场景共享）

```
wind(t, pos) = base × dir × (0.6 + 0.4·gust(t))
             + pointerSwirl(pos)
gust(t)      = fbm(t × 0.15) 阵风包络，每 6-12s 一次峰值
pointerSwirl = 鼠标速度矢量场（半径 180px，指数衰减）
```

| 消费者 | 接入方式 |
|---|---|
| 长发/裙摆刚体 | ammo 外力（见上） |
| 花瓣粒子 | 粒子 update 中采样 wind |
| 花海背景板 | 顶部草浪 band 顶点位移 shader uniform |
| 表单玻璃卡 | 不受风（保持 UI 稳定） |

风向默认自右向左（从角色吹向表单），与构图呼应。

## 7. 花瓣粒子

- InstancedMesh（单 draw call），数量桌面 160 / 移动 60。
- 每瓣属性：相位、大小 6-14px、深度 0.4-1.0（视差速度）、旋转速度。
- 运动 = 风场漂移 + 重力 0.2g + 深度视差 + 呼吸式密度（风峰时散出更多）。
- 出屏回收重生于右侧风向上游。

## 8. 花海背景板

- `task-mtzyvfxf16eyz.png` 铺满全屏（cover），置于 z 最远层。
- 顶部 20% 高度做「草浪」细分网格，顶点位移随风场 uniform 摆动。
- 昼夜通过 shader 混色（乘性色温 + 加性辉光）作用于背景板，与 3D 层
  光照统一过渡。

## 9. 昼夜系统

| 项 | 日间 | 夜间 |
|---|---|---|
| 天空调色 | 暖白→淡粉（顶部淡蓝） | 深蓝紫→暗玫 |
| 主光 | 暖白 1.0 | 月蓝 0.6 |
| 补光 | 粉 0.4 | 紫罗兰 0.3 |
| Bloom | 0.35 | 0.6（星野/发光元素更亮） |
| 星野 | 无 | 程序化星点淡入（背景板 shader） |

- 切换 = 刻印式按钮（复刻主项目：日=琥珀环/夜=月蓝环），所有参数
  1.2s ease-in-out lerp；期间可加一次「光晕扫过」转场遮罩。

## 10. 登录 UI（DOM 居左）

沿用主项目登录页的设计语言并玻璃化：

- 容器：左 40% 垂直居中，`backdrop-blur + bg-card/60` 玻璃卡。
- 标题：`Panel Access Token`（衬线小字号，同主项目）。
- 输入：下划线形态；聚焦时粉线从左及右 300ms 展开（同主项目）。
- 按钮：悬停混沌轨道粒子（移植 rAF 双频谐和轨道版，参数照旧）。
- 状态：空值/错误（`123` 之外）显示 ember 错误条；校验通过 → 成功转场。
- 成功转场分镜（总 1.8s）：风场强度渐息（0.4s）→ 白粉光晕自表单扩散
  （0.8s）→ 交叉淡化进入 /home。
- 页脚：模型出处与版权说明（神帝宇 / miHoYo，仅内部展示）。

## 11. 路由与脚手架

- `/login`：主展示页（默认路由）。
- `/home`：固定首页——静态展示（标题 + 一句欢迎语 + 返回登录按钮）。
- 校验：`token === '123'` 存 sessionStorage；`/home` 无 token 时弹回。
- 目录规划：

```
login-demo/
├─ PLAN.md
├─ public/assets/          # gitignore（模型/贴图/花海图，脚本放置）
├─ src/
│  ├─ app/                 # 路由与页面壳
│  ├─ scene/               # three 场景：stage.ts / character.ts /
│  │                       #   windfield.ts / petals.ts / sky.ts / daynight.ts
│  ├─ ui/                  # 表单、粒子按钮、下划线输入、刻印切换
│  └─ styles/
└─ scripts/fetch-assets.md # 资产放置说明
```

## 12. 性能与降级

| 项 | 预算 | 降级 |
|---|---|---|
| 帧率 | 60fps（桌面） | <45fps 持续 3s：物理 substep 3→1、花瓣 160→80 |
| DPR | ≤ 2 | — |
| reduced-motion | 检测 | 静态渲染一帧 + UI 动效全关 |
| 移动端 | 触屏可用 | 无指针视线跟随；粒子 60；物理 1 substep |

## 13. 里程碑与验收

| 阶段 | 内容 | 验收标准 |
|---|---|---|
| M0 | 脚手架 + 资产迁移 + pmx 加载 | 模型以 toon 材质正确显示 |
| M1 | 构图 + 花海背景 + 角色摆位 | 截图比对构图示意 |
| M2 | 长发物理 + 风场 | 阵风周期性吹动发梢，躯干稳定 |
| M3 | 花瓣粒子 + 指针扰动 | 鼠标划过可见风涡 |
| M4 | 昼夜 + 灯光 + 星野 | 切换 1.2s 全场景同步过渡 |
| M5 | UI + 转场 + /home | `123` 登录全流程动效完整 |
| M6 | 性能与降级 | 桌面稳定 60fps；降级路径可触发 |

## 14. 开放问题

1. 默认模型用 `爱莉希雅 3.0.pmx` 还是长裙变体？（暂定 3.0 常服）
2. 法杖.pmx 是否挂载展示？（暂定不挂，避免遮挡表单视线）
3. 花海背景图与 MMD 光照的色温统一可能需实机微调（M4 处理）。
